✅ 1. Instala Docker desde el repositorio oficial

# Asegura dependencias necesarias
sudo apt update
sudo apt install ca-certificates curl gnupg lsb-release -y

# Agrega la clave GPG de Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/debian/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Agrega el repositorio oficial
echo \
  "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] \
  https://download.docker.com/linux/debian $(lsb_release -cs) stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Instala Docker Engine
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y


Comprueba que Docker está funcionando:


✅ 2. Descargar imagen de NGINX desde Docker Hub

docker pull nginx

✅ 3. Crear el contenedor NGINX

Redirigir el puerto 8888 del host → puerto 80 del contenedor, y montar el volumen persistente:

mkdir -p website

docker run -d --name nginx_web \
  -p 8888:80 \
  -v /home/website:/usr/share/nginx/html \
  nginx


✅ 4. Crear una página HTML sencilla

echo "<!DOCTYPE html>
<html>
  <head><title>Mi Página</title></head>
  <body>
    <h1>Kevin David Sánchez Evangelista</h1>
    <p>Matrícula: 2023-XXXXX</p>
  </body>
</html>" | sudo tee /home/website/index.html


sudo nano /home/website/index.html

<!DOCTYPE html>
<html>
  <head><title>Mi Página</title></head>
  <body>
    <h1>Kevin David Sánchez Evangelista</h1>
    <p>Matrícula: 2023-XXXXX</p>
  </body>
</html>
 
✅ 5. Verificar en el navegador

http://127.0.0.1:8888


